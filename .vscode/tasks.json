{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CodingDojo | Build Debug",
            "type": "shell",
            "command": "bazel build //Main:main -c dbg",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "CodingDojo | Build Release",
            "type": "shell",
            "command": "bazel build //Main:main -c opt",
        }
    ]
}